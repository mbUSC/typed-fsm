[package]
name = "diagram-generation"
version = "0.1.0"
edition = "2021"
description = "Automatic FSM diagram generation for typed-fsm"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2.0", features = ["full", "visit"] }
quote = "1.0"
proc-macro2 = "1.0"
